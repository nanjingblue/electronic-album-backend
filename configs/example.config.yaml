Server:
  RunMode: debug
  HttpPort: 8080
  ReadTimeOut: 60
  WriteTimeOut: 60

Database:
  DBType: mysql # [mysql, sqlite]
  Username:
  Password:
  Host: 127.0.0.1:3306
  DBName: 
  TablePrefix:
  Charset: utf8mb4
  ParseTime: True
  MaxIdleConns: 10
  MaxOpenConns: 30

  SqliteDB: ./gallery.db # 如果使用 sqlite 需要填写数据库的位置 默认就在项目根目录

  RedisAddr: 127.0.0.1:6379
  RedisPassword:
  RedisDB: 0

Jwt:
  Key:

Storage:
  GalleryCoverPath:
  PicturePath:
  AvatarPath:

# 阿里云OSS对象存储
OSS:
  END_POINT:
  ACCESS_KEY_ID:
  ACCESS_KEY_SECRET:
  BUCKET:
  DOMAIN:  #如果绑定了域名可在此填写 未绑定请留空